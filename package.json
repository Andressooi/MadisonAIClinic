{
  "name": "madison-ai-clinic",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "One marketing page for Madison AI Clinic.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:size": "node scripts/check-size.mjs",
    "og": "node scripts/build-og.mjs"
  },
  "dependencies": {
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20"
  }
}
