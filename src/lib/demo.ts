/**
 * The site has one booking surface: the closing section, which holds the
 * demo request form. Every CTA — nav, hero panel, closing — routes here, so
 * there is a single place to swap in a scheduler later if we outgrow a form.
 */
export const DEMO_HREF = '#demo'
